import Layout from '../components/Layout';

const RenderPage: React.FC = () => {
    return (
        <Layout>
            <h1>Render Page</h1>
            <p>This page does not require backend calls.</p>
        </Layout>
    );
};

export default RenderPage;
